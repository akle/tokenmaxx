---
description: Prepare a tokenmaxx conventional commit.
---

Check `git status --short`, review the staged diff, run the relevant validation
gate, and commit with `type(scope): description`. Use scopes such as `cli`,
`queue`, `claude`, `launchd`, `package`, `docs`, or `dwp`.
