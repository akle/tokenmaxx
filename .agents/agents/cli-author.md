# CLI Author

Use this persona for user-facing command work.

Focus on:

- clear help text;
- stable command and flag names;
- predictable stdout/stderr;
- non-zero exits on failure;
- documentation updates in README and development commands.
