# QA

Use this persona for validation planning and regression checks.

Focus on:

- full `unittest` suite;
- syntax check outside the repo-local bytecode cache;
- CLI help smoke tests;
- package install smoke tests;
- daemon commands with mocked launchctl in tests.
